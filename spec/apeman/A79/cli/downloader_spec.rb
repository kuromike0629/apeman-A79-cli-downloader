RSpec.describe Apeman::A79::Cli::Downloader do
  it "has a version number" do
    expect(Apeman::A79::Cli::Downloader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
